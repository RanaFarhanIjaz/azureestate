<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.azureestate">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>

    <application
        android:enableOnBackInvokedCallback="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AzureEstate">

        <!-- Splash Activity (LAUNCHER) -->
        <activity
            android:name=".home.SplashActivity"
            android:theme="@style/Theme.AzureEstate.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".auth.LoginActivity"
            android:theme="@style/Theme.AzureEstate"
            android:exported="false" />

        <!-- Main Activity (after login) -->
        <activity
            android:name=".home.MainActivity"
            android:theme="@style/Theme.AzureEstate"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />

        <activity
            android:name=".auth.SignUpActivity"
            android:theme="@style/Theme.AzureEstate"
            android:exported="false" />

        <activity
            android:name=".property.UploadPropertyActivity"
            android:theme="@style/Theme.AzureEstate"
            android:windowSoftInputMode="adjustResize"
            android:exported="false"/>

        <activity
            android:name=".chat.ChatRoomActivity"
            android:theme="@style/Theme.AzureEstate"
            android:windowSoftInputMode="adjustResize"
            android:exported="false"/>

        <activity
            android:name=".legal.LegalActivity"
            android:theme="@style/Theme.AzureEstate"
            android:exported="false"/>

        <activity
            android:name=".data.ContactSalesActivity"
            android:theme="@style/Theme.AzureEstate"
            android:exported="false"/>

        <activity
            android:name=".ai.PricePredictorActivity"
            android:theme="@style/Theme.AzureEstate"
            android:windowSoftInputMode="adjustResize"
            android:exported="false"/>

    </application>

</manifest>